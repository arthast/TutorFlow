apiVersion: v1
kind: Service
metadata:
  name: lesson-service
spec:
  selector: {app.kubernetes.io/name: lesson-service}
  ports:
    - {name: http, port: 8082, targetPort: http}
    - {name: grpc, port: 9082, targetPort: grpc}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lesson-service
spec:
  replicas: 1
  selector:
    matchLabels: {app.kubernetes.io/name: lesson-service}
  template:
    metadata:
      labels: {app.kubernetes.io/name: lesson-service, app.kubernetes.io/part-of: tutorflow}
    spec:
      containers:
        - name: lesson-service
          image: ghcr.io/arthast/tutorflow-lesson-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - {name: http, containerPort: 8082}
            - {name: grpc, containerPort: 9082}
          env:
            - name: LESSON_DATABASE_URL
              valueFrom: {secretKeyRef: {name: tutorflow-secrets, key: LESSON_DATABASE_URL}}
            - name: KAFKA_SECDIST_CONFIG
              valueFrom: {secretKeyRef: {name: tutorflow-secrets, key: KAFKA_SECDIST_CONFIG}}
          startupProbe:
            httpGet: {path: /health, port: http}
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 36
          livenessProbe:
            httpGet: {path: /health, port: http}
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          readinessProbe:
            httpGet: {path: /ready, port: http}
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: 500m, memory: 512Mi}

