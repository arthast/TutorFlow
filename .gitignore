# === Секреты и локальная конфигурация ===
.env
*.env
!.env.example

# === Локальная конфигурация (не коммитим) ===
# docs/ и AGENTS.md теперь ОТСЛЕЖИВАЮТСЯ (едут с веткой/клоном, не теряются при
# переключении). Локальными остаются только *.local.md (напр. AGENTS.local.md).
*.local.md
local/
**/local/

# Локальные настройки агентских инструментов
.claude/settings.local.json
.codex/

# === Локальное файловое хранилище file-service (MVP volume) ===
storage/
data/files/

# === Сборка C++ / CMake / userver ===
build/
**/build/
cmake-build-*/
CMakeCache.txt
CMakeFiles/
CMakeUserPresets.json
*.o
*.obj
*.a
*.so
*.dylib
compile_commands.json

# === Менеджеры зависимостей ===
conan/
.conan/
conandata.yml.lock
vcpkg_installed/
_deps/

# === Python test artifacts ===
__pycache__/
**/__pycache__/
*.pyc

# === IDE / редакторы ===
.idea/
.vscode/
*.swp
*.swo

# === ОС ===
.DS_Store
Thumbs.db
