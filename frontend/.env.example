# URL api-gateway (REST-команды и чтение домена). Фронт никогда не ходит во внутренние сервисы.
VITE_API_URL=http://localhost:8080

# URL realtime-service (WebSocket push only).
VITE_REALTIME_URL=ws://localhost:8089/ws
